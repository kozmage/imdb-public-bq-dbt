version: 2

models:
  - name: stg_title_basics
    description: Basic Movie INFO, filtered movies only.
    columns:
      - name: movie_id
        description: PK 
      - name: title
        description: Movie title in English
      - name: year
        description: Creation year
      - name: runtime_minutes
        description: Runtime in minutes
      - name: genres_arr
        description: Array of genres of the movie.